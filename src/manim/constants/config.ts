/**
 * Manim configuration constants
 * Frame dimensions based on Manim's default 8 units tall, 14.22 units wide
 */

// Frame dimensions in Manim units
export const FRAME_HEIGHT = 8;
export const FRAME_WIDTH = 14.222222;
export const FRAME_Y_RADIUS = FRAME_HEIGHT / 2;
export const FRAME_X_RADIUS = FRAME_WIDTH / 2;

// Default video settings
export const DEFAULT_PIXEL_WIDTH = 1920;
export const DEFAULT_PIXEL_HEIGHT = 1080;
export const DEFAULT_FPS = 60;

// Stroke defaults
export const DEFAULT_STROKE_WIDTH = 4;

// Animation defaults
export const DEFAULT_ANIMATION_RUN_TIME = 1; // seconds
export const DEFAULT_WAIT_TIME = 1; // seconds

// Dot settings
export const DEFAULT_DOT_RADIUS = 0.08;
export const DEFAULT_SMALL_DOT_RADIUS = 0.04;

// Arrow settings
export const DEFAULT_ARROW_TIP_LENGTH = 0.35;
export const DEFAULT_ARROW_TIP_WIDTH = 0.35;

// Text settings
export const DEFAULT_FONT_SIZE = 48;
export const DEFAULT_LINE_SPACING = 0.3;

// Number precision
export const NUM_DECIMAL_PLACES = 2;
